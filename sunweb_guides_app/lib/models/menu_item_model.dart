import 'package:flutter/material.dart';

class MenuItem {
  IconData icon;
  String name;
  String path;

  MenuItem (
      {
        required this.icon,
        required this.name,
        required this.path
     });
}